# Team Runtime Operations

Owns desktop IPC for runtime logs, diagnostics, lifecycle commands, and process
termination. Application use cases depend on narrow ports; Electron and main
process services are composed only in the feature main layer.
